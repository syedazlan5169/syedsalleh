# --- PHPUnit cache (anywhere, including backend/) ---
.phpunit.cache
.phpunit.result.cache

# --- Dependencies (anywhere) ---
vendor/
node_modules/

# --- Laravel env & config ---
.env
.env.backup
.env.production
.env.*          # ignore other env variants
.phpactor.json

# --- Laravel storage & public build (backend app) ---
backend/public/build
backend/public/hot
backend/public/storage

backend/storage/*.key
backend/storage/pail
backend/storage/framework/*
backend/storage/logs/*

# --- Homestead / local dev ---
Homestead.json
Homestead.yaml

# --- NPM / Yarn log files ---
npm-debug.log
yarn-error.log

# --- Auth / private config ---
/auth.json

# --- IDE / editor folders ---
.fleet
.idea
.nova
.vscode
.zed

# --- Mobile app (Expo) ignores ---
mobile/.expo/
mobile/.expo-shared/
mobile/dist/
mobile/web-build/
mobile/node_modules/

# If later you run `expo prebuild`, these native folders will appear; ignore them too:
mobile/android/
mobile/ios/

